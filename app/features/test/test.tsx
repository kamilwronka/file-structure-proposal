import { Button } from "~/components";
import { PerFeatureComponent } from "~/features/test/components/per-feature-component/per-feature-component";

export const Test = () => {
  return (
    <div>
      <PerFeatureComponent />
      <Button />
    </div>
  );
};
